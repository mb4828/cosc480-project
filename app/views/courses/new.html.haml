-# this template handles course creation on a schedule

%p= flash[:notice] if flash[:notice]
%p= flash[:warning] if flash[:warning]

%h1 Add a new course

%div.row
  = render partial: "layouts/schedule_form_new_course", locals: {course: @course, schedule: @schedule}
