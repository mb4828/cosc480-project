%h1 Create (edit) a schedule

%div.row
  = debug(params)
  = debug(@courses)
  = debug(@schedule)
  = render "layouts/schedule_viz"
  = render partial: "layouts/schedule_form_list", locals: {courses: @courses, schedule: @schedule}
    
